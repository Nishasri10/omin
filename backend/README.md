# Backend

Express API for the POS omnichannel system.

## Getting started

1. Install dependencies: `npm install`
2. Start backend: `npm run dev`

## Environment

Configure `backend/.env` with MongoDB, Redis, JWT, and frontend URL.
